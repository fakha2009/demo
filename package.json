{
  "name": "chemlab-tj-frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "node scripts/generate-config.mjs",
    "dev": "py -m http.server 5173",
    "start": "py -m http.server 5173",
    "backend": "cd backend && go run ./cmd/api",
    "seed": "cd backend && powershell -ExecutionPolicy Bypass -File scripts/seed.ps1"
  }
}
